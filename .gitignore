# SSL 인증서 (자체 서명 인증서는 서버에서 직접 생성)
ssl/*.crt
ssl/*.key

# 환경변수 파일 (민감 정보 포함)
.env
backend/.env

# MCP 설정 (API 키 포함)
.mcp.json

# Node.js
node_modules/
npm-debug.log*

# 테스트/임시 파일
*.log
*.png
tmp_*
nul
.playwright-mcp/

# 부하테스트 생성 토큰 (보안)
load-tests/**/tokens-*.csv

# 로컬 Docker 테스트 파일
docker-compose.local.yml
schedule-frontend/Dockerfile.local
schedule-frontend/nginx.local.conf
